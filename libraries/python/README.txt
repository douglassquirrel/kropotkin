API client for the Kropotkin framework. See the Kropotkin distribution for
more details: https://github.com/douglassquirrel/kropotkin.

To install from the Kropotkin distribution:
./build.py
cd dist
./install.sh

To install when distributed independently:
tar xf kropotkin-*.tar
cd kropotkin-*/
sudo python setup.py install
